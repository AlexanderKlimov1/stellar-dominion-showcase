package com.moo3.server.domain.enums;

/**
 * Кем занято место в партии.
 * <p>
 * <b>Третий род — не игрок вовсе.</b> {@link #MONSTER} это строка, за которой стоит
 * космическое чудище (п. 11.1): у него нет ни колоний, ни науки, ни дипломатии, и
 * появилось оно лишь потому, что тактический бой ведётся между двумя СТОРОНАМИ, а у
 * стороны в этой игре есть владелец. Чудище империей не считается нигде: списки
 * участников партии берутся {@code PlayerRepository.findEmpiresByGameIdOrderBySlotAsc},
 * и эта выборка его не отдаёт. Полный список, вместе с чудищем, нужен только бою.
 */
public enum PlayerType {
    HUMAN,
    AI,
    MONSTER
}
