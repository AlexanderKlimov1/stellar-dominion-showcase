package com.moo3.server.dto;

import java.util.List;

/**
 * Всё, что показывает окно «Инфо» — п. 11.1.
 * <p>
 * В MOO II окно Information даёт четыре вещи: график, на котором своя империя стоит рядом
 * с соперниками; свойства рас всех империй, с которыми игрок в контакте; изученные
 * технологии; описания всех технологий вообще. Первые три приходят отсюда одним запросом:
 * экран открывается целиком, и три отдельных запроса рисовали бы его частями. Четвёртое —
 * само дерево технологий, оно уже есть у клиента справочником.
 *
 * @param turn    ход, на который снят срез: до него доходит и график
 * @param empires империи, о которых игроку позволено знать: своя и знакомые (п. 15)
 */
public record EmpireInfoDto(
        Integer turn,
        List<EmpireProfileDto> empires
) {
}
