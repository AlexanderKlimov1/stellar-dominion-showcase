package com.moo3.server.dto;

/**
 * Оружие корабля в карточке экрана флота — п. 8.
 * <p>
 * Окно Fleet Operations MOO II пишет вооружение строкой «2 Nuclear Missiles (360)»:
 * сколько стволов, чем и какой у них общий урон. Урон здесь тот же, что копится в залп
 * проекта, — урон ствола на число выстрелов и на число стволов, — но без приборов и расы:
 * приборы и раса уже учтены в общем залпе корабля, и складывать их ещё и в каждую строку
 * значило бы посчитать дважды.
 *
 * @param name   название компонента из справочника кораблей
 * @param count  сколько таких стволов на корабле
 * @param damage урон всех этих стволов за один заход
 */
public record FleetWeaponDto(
        String name,
        Integer count,
        Integer damage
) {
}
