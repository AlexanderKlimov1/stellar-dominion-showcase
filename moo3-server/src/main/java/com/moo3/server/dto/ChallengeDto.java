package com.moo3.server.dto;

/**
 * Задачка перед регистрацией — п. 3.1: вопрос и его номер.
 * <p>
 * Ответа здесь нет и быть не может: он остаётся у сервера, иначе задачка решалась бы
 * чтением собственного ответа.
 *
 * @param id       номер вопроса: с ним ответ и приходит обратно в заявке
 * @param question сам вопрос, готовый к показу
 * @param hint     чем отвечать — подпись под полем ответа
 */
public record ChallengeDto(
        String id,
        String question,
        String hint
) {
}
