package com.moo3.server.dto;

import java.util.UUID;

/**
 * Куда назначить лидера — п. 6.
 * <p>
 * Одно поле на оба рода: колониальному это звёздная система, корабельному — флот. Пусто
 * значит «вернуть в резерв»: снятие с должности — то же назначение, только никуда.
 *
 * @param targetId система или флот; пусто — обратно в офицерский резерв
 */
public record AssignLeaderRequest(UUID targetId) {
}
