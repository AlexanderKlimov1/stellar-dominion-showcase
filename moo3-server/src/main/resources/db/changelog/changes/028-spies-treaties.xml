<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!--
        Шпионы поимённо и договоры империй — п. 13 и п. 15.

        Раньше шпионы были числом на игроке: их некуда было отправлять. Теперь у каждого
        агента своё задание и свои накопленные очки, поэтому у него появилась строка.
        Колонка player.spies остаётся: по ней считается, сколько агентов работает дома,
        и она же хранит их для старых сохранений.

        Договоры лежат колонкой в отношениях: набор небольшой, читается всегда вместе
        с самими отношениями, и искать по нему нечего.
    -->
    <changeSet id="028-01-create-spy" author="moo3">
        <createTable tableName="spy">
            <column name="id" type="UUID">
                <constraints primaryKey="true" primaryKeyName="pk_spy" nullable="false"/>
            </column>
            <column name="owner_player_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="target_player_id" type="UUID"/>
            <column name="mission" type="VARCHAR(32)">
                <constraints nullable="false"/>
            </column>
            <column name="points" type="INT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="created_turn" type="INT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint baseTableName="spy" baseColumnNames="owner_player_id"
                                 referencedTableName="player" referencedColumnNames="id"
                                 constraintName="fk_spy_owner" onDelete="CASCADE"/>
        <createIndex tableName="spy" indexName="ix_spy_owner">
            <column name="owner_player_id"/>
        </createIndex>
    </changeSet>

    <changeSet id="028-02-add-relation-treaties" author="moo3">
        <addColumn tableName="diplomacy_relation">
            <column name="treaties" type="VARCHAR(256)"/>
        </addColumn>
    </changeSet>

</databaseChangeLog>
